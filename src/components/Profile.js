import React from 'react';
import {ProfileInfo} from './ProfileInfo';

export const Profile = () => {
    return (
        <div>
            Profile

            <ProfileInfo />
            
        </div>
    )
}